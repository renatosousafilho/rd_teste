<%= simple_form_for @custom_field do |f| %>
  <%= f.input :name %>
  <%= f.input :kind, as: :select, collection: CustomField.kinds.keys, prompt: "Selecione um tipo"  %>
  <%= f.submit 'Save' %>
<% end %>
